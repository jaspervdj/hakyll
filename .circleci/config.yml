version: 2
jobs:
  build:
    docker:
      - image: 'fpco/stack-build:lts'
    steps:
      - checkout
      - run:
          name: 'Install GHC'
          command: 'stack --no-terminal --skip-ghc-check setup'
      - run:
          name: 'Run tests'
          command: 'stack --not-terminal --skip-ghc-check test'
