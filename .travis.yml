language: haskell
ghc: '8.0.2'
sudo: false

cache:
  directories:
  - '$HOME/.cabal'
  - '$HOME/.ghc'

branches:
  only:
    - 'master'

install:
  - './scripts/tickle.sh cabal install --only-dependencies --enable-tests'

script:
  - 'cabal test'
