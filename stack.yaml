resolver: lts-10.6
extra-package-dbs: []

flags:
  hakyll:
    previewServer: True
    watchServer:   True
    checkExternal: True
    buildWebsite:  True

packages:
  - '.'

extra-deps:
  - 'pandoc-citeproc-0.13.0.1'

nix:
  enable: false
  packages:
    - zlib
